config:
  target: "http://localhost:3000"
  phases:
    - duration: 30
      arrivalRate: 10
      name: "Baseline load test"
  defaults:
    headers:
      Content-Type: "application/json"

scenarios:
  - name: "GET all posts"
    flow:
      - get:
          url: "/posts"